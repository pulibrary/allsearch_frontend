# Agents Guide for Allsearch Frontend

This application is the frontend for the [Princeton University Library All Search](https://allsearch.princeton.edu/).

This uses yarn as its package manager.

# Commands

Be sure your changes pass the following:
* `yarn test` to run tests
* `yarn build` to confirm that it builds without error
* `bundle exec rspec` to run integration tests

